class {{name}}Migration extends Vakoo.Migration

  constructor: -> super

  connectionName: "{{connectionName}}"

  up: (callback)->

    callback()

  down: (callback)->

    callback()


module.exports = {{name}}Migration